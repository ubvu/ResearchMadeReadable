
# =============================================================================
# Research made Readable - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# AbacusAI API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# The AbacusAI API key provides access to all AI models used in this application
# including GPT-4, Claude, Deepseek, Llama, and Mistral through a unified API
# 
# To get your API key:
# 1. Visit https://abacus.ai/
# 2. Sign up for an account or log in
# 3. Navigate to your account settings or API section
# 4. Generate a new API key
# 5. Copy the key and paste it below (without quotes)
#
# Example format: abacus_1234567890abcdef1234567890abcdef
ABACUSAI_API_KEY=your_abacusai_api_key_here

# -----------------------------------------------------------------------------
# Application Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# These settings can be customized but have reasonable defaults

# Streamlit app settings
# STREAMLIT_SERVER_PORT=8501
# STREAMLIT_SERVER_ADDRESS=localhost

# Data storage paths (relative to project root)
# DATA_DIR=data
# UPLOADS_DIR=data/uploads
# EXPORTS_DIR=data/exports
# DB_DIR=data/db

# Logging configuration
# LOG_LEVEL=INFO
# LOG_FILE=streamlit.log

# AI Model Configuration
# DEFAULT_AI_MODEL=GPT-4-Mini
# DEFAULT_TEMPERATURE=0.7
# MAX_TOKENS=500

# -----------------------------------------------------------------------------
# Performance and Limits (OPTIONAL)
# -----------------------------------------------------------------------------
# These settings help control resource usage and API costs

# Maximum file size for uploads (in MB)
# MAX_UPLOAD_SIZE=10

# API request timeout (in seconds)
# API_TIMEOUT=30

# Maximum number of concurrent API requests
# MAX_CONCURRENT_REQUESTS=5

# -----------------------------------------------------------------------------
# Development and Debug Settings (OPTIONAL)
# -----------------------------------------------------------------------------
# Only needed for development and debugging

# Enable debug mode (shows additional logging)
# DEBUG=False

# Enable verbose logging
# VERBOSE=False

# Skip API key validation (for testing UI without API access)
# SKIP_API_VALIDATION=False

# -----------------------------------------------------------------------------
# IMPORTANT NOTES:
# -----------------------------------------------------------------------------
# 1. NEVER commit the actual .env file to version control
# 2. Keep your API key secure and never share it publicly
# 3. The AbacusAI API may have usage limits and associated costs
# 4. Only the ABACUSAI_API_KEY is required - all other settings are optional
# 5. Copy this file to .env and fill in your actual values
# 6. Restart the application after making changes to environment variables
# =============================================================================
